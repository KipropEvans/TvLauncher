<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/black" android:orientation="vertical"
    android:paddingStart="60dp" android:paddingEnd="60dp" android:paddingTop="40dp">

    <EditText android:id="@+id/search_box"
        android:layout_width="500dp" android:layout_height="wrap_content"
        android:hint="Search movies, shows…" android:textColorHint="@color/light_gray"
        android:textColor="@color/white" android:backgroundTint="@color/light_gray"
        android:imeOptions="actionSearch" android:inputType="text"
        android:visibility="gone"/>

    <TextView android:id="@+id/page_title"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:textColor="@color/white" android:textSize="42sp" android:textStyle="bold"
        android:layout_marginBottom="28dp"/>

    <androidx.recyclerview.widget.RecyclerView android:id="@+id/page_shelves"
        android:layout_width="match_parent" android:layout_height="match_parent"
        android:clipToPadding="false"/>
</LinearLayout>